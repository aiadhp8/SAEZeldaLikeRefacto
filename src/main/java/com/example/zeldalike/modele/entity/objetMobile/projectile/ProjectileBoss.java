package com.example.zeldalike.modele.entity.objetMobile.projectile;

import com.example.zeldalike.modele.entity.Position;

public class ProjectileBoss extends Projectile{

    public ProjectileBoss(Position position, int direction, int vitesse, int degats) {
        super(position, direction, vitesse, degats);
    }
}
